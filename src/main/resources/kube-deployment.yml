apiVersion: apps/v1
kind: Deployment
metadata:
  name: hub-imageinspector-ws
  labels:
    app: hub-imageinspector-ws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hub-imageinspector-ws
  template:
    metadata:
      labels:
        app: hub-imageinspector-ws
    spec:
      containers:
      - name: hub-imageinspector-ws-alpine
        image: blackducksoftware/hub-imageinspector-ws-alpine:1.0.1-SNAPSHOT
        volumeMounts:
        - name: inspector-target-dir
          mountPath: /opt/blackduck/hub-imageinspector-ws/target
        - name: inspector-working-dir
          mountPath: /opt/blackduck/hub-imageinspector-ws/working
        ports:
        - containerPort: 8080
          hostPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 120
          periodSeconds: 30
      - name: hub-imageinspector-ws-centos
        image: blackducksoftware/hub-imageinspector-ws-centos:1.0.1-SNAPSHOT
        volumeMounts:
        - name: inspector-target-dir
          mountPath: /opt/blackduck/hub-imageinspector-ws/target
        ports:
        - containerPort: 8081
          hostPort: 8081
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 120
          periodSeconds: 30
      - name: hub-imageinspector-ws-ubuntu
        image: blackducksoftware/hub-imageinspector-ws-ubuntu:1.0.1-SNAPSHOT
        volumeMounts:
        - name: inspector-target-dir
          mountPath: /opt/blackduck/hub-imageinspector-ws/target
        ports:
        - containerPort: 8082
          hostPort: 8082
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 120
          periodSeconds: 30
      volumes:
      - name: inspector-target-dir
        hostPath:
          path: /Users/billings/tmp/target
          type: Directory
      - name: inspector-working-dir
        hostPath:
          path: /Users/billings/tmp/working
          type: Directory
