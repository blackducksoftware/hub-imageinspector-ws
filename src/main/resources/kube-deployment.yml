apiVersion: apps/v1
kind: Deployment
metadata:
  name: hub-imageinspector-ws
  labels:
    app: hub-imageinspector-ws
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hub-imageinspector-ws
  template:
    metadata:
      labels:
        app: hub-imageinspector-ws
    spec:
      containers:
      - name: hub-imageinspector-ws-alpine
        image: blackducksoftware/hub-imageinspector-ws-alpine:2.1.1
        volumeMounts:
        - name: inspector-shared-dir
          mountPath: /opt/blackduck/shared
        ports:
        - containerPort: 8080
          hostPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 120
          periodSeconds: 120
          timeoutSeconds: 60
      - name: hub-imageinspector-ws-centos
        image: blackducksoftware/hub-imageinspector-ws-centos:2.1.1
        volumeMounts:
        - name: inspector-shared-dir
          mountPath: /opt/blackduck/shared
        ports:
        - containerPort: 8081
          hostPort: 8081
        livenessProbe:
          httpGet:
            path: /health
            port: 8081
          initialDelaySeconds: 120
          periodSeconds: 120
          timeoutSeconds: 60
      - name: hub-imageinspector-ws-ubuntu
        image: blackducksoftware/hub-imageinspector-ws-ubuntu:2.1.1
        volumeMounts:
        - name: inspector-shared-dir
          mountPath: /opt/blackduck/shared
        ports:
        - containerPort: 8082
          hostPort: 8082
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 120
          periodSeconds: 120
          timeoutSeconds: 60
      volumes:
      - name: inspector-shared-dir
        hostPath:
          path: /Users/billings/tmp/shared
          type: Directory
